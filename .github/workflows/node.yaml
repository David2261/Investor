name: Node CI/CD
on:
  push:
    branches: ["frontend"]
  pull_request:
    branches: ["frontend"]
jobs:
  check_node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: Get the directory code
      - name: Run actions
        run: ./frontend/.github/workflows/frontend.yaml
