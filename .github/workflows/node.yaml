name: Node CI/CD
on: push
jobs:
  check_node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: Get the directory code
      - name: Add directory
        uses: ./frontend/.github/workflows/actions.yml
