name: Django CI
# workflow_dispatch

# Событие при котором запускается Trigger
on: [workflow_dispatch]

# Задача
jobs:
  Output:
    runs-on: ubuntu-latest
    steps:
      - name: Output to console
        run: echo This shts work
